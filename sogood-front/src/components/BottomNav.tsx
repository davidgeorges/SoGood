import React from "react";

const BottomNav: React.FC = () => (
  <nav className="fixed bottom-0 left-0 right-0 bg-white flex justify-around items-center py-3 shadow-xl rounded-t-2xl">
    {/* ... SVG buttons ... */}
    {/* Copie-colle ici les 5 boutons SVG comme dans le JS */}
  </nav>
);

export default BottomNav;
